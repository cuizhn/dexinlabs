# ADR-0001：项目哲学 — 长期稳定维护优先

| 字段 | 值 |
|------|-----|
| **ADR 编号** | 0001 |
| **标题** | 项目哲学 — 长期稳定维护优先 |
| **状态** | 生效中 |
| **创建日期** | 2026-07-16 |
| **架构版本** | Architecture V2 |
| **相关文档** | [ARCHITECTURE.md](../Architecture/ARCHITECTURE.md) |

---

## 一、背景（Context）

得心实验室教育平台在发展初期，曾尝试"自主实现一切"的技术路线：

- 自行实现 Markdown Parser / Lexer / AST
- 自行封装多层 ORM 抽象
- 自行构建复杂的中间层（Mapper / Entity / DTO 转换）
- 自行实现各种基础设施能力

随着项目演进，这种路线带来了以下问题：

1. **维护成本高** — 自行实现的 Parser、Lexer 需要持续跟进 Markdown 规范变化
2. **问题定位难** — 多层抽象导致问题链路长，难以快速定位根因
3. **新人上手慢** — 大量自研概念和架构，学习曲线陡峭
4. **生态脱节** — 无法直接复用成熟的社区插件和工具
5. **业务产出低** — 大量精力消耗在基础设施，而非教育业务本身

实践证明：对于一个教育平台而言，核心价值在于**教育内容和学习体验**，而非底层技术实现。

---

## 二、决策（Decision）

确立项目核心哲学：

> **得心实验室不是一个追求"自主实现一切"的项目，
> 而是一个追求"长期稳定维护"的项目。**
>
> **所有架构决策，以降低维护成本、提高可理解性、快速定位问题为最高优先级。**

具体决策如下：

### 决策 1：成熟方案优先

凡是已有成熟开源方案的能力，优先采用社区方案，不重复造轮子。

| 能力 | 采用方案 | 理由 |
|------|----------|------|
| Markdown 解析 | remark + unified 生态 | 社区最活跃、插件最丰富的 Markdown 生态 |
| 数学公式 | KaTeX | 渲染速度快、社区成熟 |
| 数据库 ORM | Drizzle ORM | 类型安全、性能优异、社区活跃 |
| 数据库 | PostgreSQL (Neon) | Serverless、运维成本低 |

### 决策 2：只实现业务

项目维护的代码，只负责真正的教育业务逻辑：

- 课程内容组织
- 学习流程设计
- 练习系统
- 学习记录
- 权限控制

基础设施全部交给成熟生态。

### 决策 3：Engine 是协调者

Engine 的定位是**能力协调者（Coordinator）**，不是重复实现者：

- ✅ 负责：配置、组合、协调、统一 API
- ❌ 不负责：重新实现 Parser / Lexer / ORM / Storage

### 决策 4：避免无意义抽象

不为了"架构好看"而增加层次。

判断标准：如果某一层只是 `A → B` 的参数透传，没有增加任何业务价值，应删除。

反模式：
- Repository → Mapper → Entity → DTO → Service（层层转发）
- 为了"遵循某某模式"而增加的空壳层

---

## 三、后果（Consequences）

### 正面后果

1. **维护成本降低** — 依托成熟社区，自行维护的代码量大幅减少
2. **问题定位更快** — 高内聚低耦合，问题可快速定位到唯一模块
3. **新人上手更快** — 标准技术栈，社区文档丰富
4. **业务产出更高** — 团队精力集中在教育业务本身
5. **生态兼容性好** — 可直接复用 remark 插件、Drizzle 工具链等

### 负面后果

1. **依赖外部生态** — 需要关注上游依赖的版本迭代和 breaking change
2. **定制化受限** — 深度定制可能需要 fork 或编写插件
3. **选型风险** — 若上游项目停止维护，需要寻找替代方案

### 缓解措施

1. 选择**社区活跃、使用广泛**的成熟方案，降低选型风险
2. 通过 Engine 层封装，**对外保持稳定 API**，内部实现可替换
3. 关注上游**版本发布和变更日志**，预留升级缓冲期

---

## 四、判断准则（Decision Criteria）

今后新增任何代码前，请依次判断：

### 问题 1：是否已有成熟方案？

- 如果有 → 优先用成熟方案
- 如果没有 → 继续判断

### 问题 2：是否只是重复封装？

- 如果只是给成熟方案套一层壳，没有增加业务价值 → 不要加
- 如果确实增加了业务逻辑（如统一配置、组合能力） → 可以加

### 问题 3：是否真正属于得心实验室业务？

- 如果是教育业务相关（课程、学习、练习等） → 自己实现
- 如果是通用基础设施（解析、存储、渲染等） → 用成熟方案

### 问题 4：是否增加了维护成本？

- 如果增加了大量需要长期维护的代码 → 重新考虑
- 如果只是薄封装，维护成本低 → 可以接受

### 问题 5：出现问题时，能否快速定位到唯一模块？

- 如果职责清晰，问题可定位 → 合格
- 如果职责交叉，问题链路长 → 重新设计

---

## 五、相关原则（Related Principles）

本 ADR 确立的哲学，是以下架构原则的顶层指导：

| 原则 | 说明 | 位置 |
|------|------|------|
| 成熟方案优先 | 能用社区方案就不用自己写 | [ARCHITECTURE.md](../Architecture/ARCHITECTURE.md) 原则 1 |
| 只实现业务 | 业务代码和基础设施代码严格区分 | [ARCHITECTURE.md](../Architecture/ARCHITECTURE.md) 原则 2 |
| Engine 是协调者 | Engine 组织能力，不重复实现 | [ARCHITECTURE.md](../Architecture/ARCHITECTURE.md) 原则 3 |
| 避免无意义抽象 | 不为分层而分层 | [ARCHITECTURE.md](../Architecture/ARCHITECTURE.md) 原则 4 |
| 高内聚低耦合 | 同能力同目录，上层不知下层细节 | [ARCHITECTURE.md](../Architecture/ARCHITECTURE.md) 原则 5-6 |
| 业务优先 | 业务需求决定技术方案 | [ARCHITECTURE.md](../Architecture/ARCHITECTURE.md) 原则 7 |

---

## 六、一句话总结

> **业务自己实现。**
> **基础能力交给成熟生态。**
> **Engine 负责组织，而不是重复实现。**
