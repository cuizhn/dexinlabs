# 一、业务模型

你的当前业务模型定义为：

```text
Chapter（学习单元）
 ↓
Lesson（课时内容）
 ↓
Exercise（章节练习）
```

其中：

```text
Chapter = 一元一次方程、二次函数、圆
Lesson = 入门介绍、顶点公式、图像性质
Exercise = 本章练习
```

---

# 二、路由结构

路由仅表示网站功能区，不代表数据模型。

```text
/course/:chapter/:lesson
/exercise/:chapter
```

例如：

```text
/ course / quadratic-function / introduction
```

这里的：

```text
course
```

仅仅是：

```text
学习内容区（Learning Area）
```

不代表 Content 中必须存在 course 集合。

---

# 三、Content 目录

推荐结构：

```text
content/

chapter/
  quadratic-equation.yml
  quadratic-function.yml
  circle.yml

lesson/
  quadratic-function-introduction.md
  quadratic-function-vertex.md
  circle-introduction.md

exercise/
  quadratic-function.yml
  circle.yml
```

---

# 四、Chapter 结构

Chapter 是核心组织单元。

例如：

```yaml
title: 二次函数
slug: quadratic-function

intro: quadratic-function-introduction

exercise: quadratic-function

lessons:
  - quadratic-function-introduction
  - quadratic-function-vertex
  - quadratic-function-graph
```

Chapter 负责：

```text
定义章节
定义课时顺序
定义章节入口
定义章节练习
```

---

# 五、Lesson 结构

Lesson 只负责内容。

例如：

```md
---
title: 顶点公式
slug: quadratic-function-vertex
order: 2
---

# 顶点公式
...
```

Lesson 不负责组织关系。

---

# 六、Exercise 结构

每个 Chapter 对应一个 Exercise。

例如：

```yaml
title: 二次函数练习
slug: quadratic-function

questions:
  - q1
  - q2
  - q3
```

---

# 七、API 结构

推荐：

```text
server/api/

chapter/
├── index.get.ts
└── [slug].get.ts

lesson/
└── [slug].get.ts

exercise/
└── [slug].get.ts
```

---

# 八、API 职责

## chapter/index.get.ts

获取章节列表。

```text
GET /api/chapter
```

返回：

```text
全部章节
```

用于：

```text
课程首页
章节导航
```

---

## chapter/[slug].get.ts

获取单个章节完整数据。

```text
GET /api/chapter/quadratic-function
```

返回：

```json
{
  "chapter": {},
  "lessons": [],
  "exercise": {}
}
```

用于：

```text
章节首页
章节目录
章节学习入口
```

---

## lesson/[slug].get.ts

获取单个课时内容。

```text
GET /api/lesson/quadratic-function-introduction
```

返回：

```json
{
  "lesson": {}
}
```

---

## exercise/[slug].get.ts

获取章节练习。

```text
GET /api/exercise/quadratic-function
```

返回：

```json
{
  "exercise": {}
}
```

---

# 九、Repository 层

推荐独立目录：

```text
repositories/

ChapterRepository.ts
LessonRepository.ts
ExerciseRepository.ts
```

不放在模块目录内。

---

职责：

```text
统一数据访问
封装 queryCollection
未来切换数据源
```

---

# 十、前端调用层级

统一采用：

```text
Vue Page
 ↓
Composable
 ↓
API
 ↓
Repository
 ↓
Content
```

例如：

```text
pages
 ↓
useChapter
 ↓
/api/chapter/[slug]
 ↓
ChapterRepository
 ↓
queryCollection
```

---

# 十一、Chapter 与 Lesson 的关系

采用：

```text
Chapter → Lesson
```

单向组织。

Chapter 负责维护：

```yaml
lessons:
  - lesson-a
  - lesson-b
  - lesson-c
```

Lesson 不维护归属关系。

---

# 十二、Lesson Slug 规范

采用全局唯一。

例如：

```text
quadratic-function-introduction
quadratic-function-vertex
circle-introduction
```

这样：

```text
/api/lesson/[slug]
```

可以直接访问。

---

# 十三、未来学科扩展

当前无需：

```text
course.yml
subject.yml
```

未来扩展物理、化学时再引入：

```text
Subject
 ↓
Chapter
 ↓
Lesson
```

当前阶段保持：

```text
Chapter
 ↓
Lesson
 ↓
Exercise
```

即可。

---

# 十四、最终目录结构

```text
content/
├── chapter/
│   ├── quadratic-equation.yml
│   ├── quadratic-function.yml
│   └── circle.yml
│
├── lesson/
│   ├── quadratic-function-introduction.md
│   ├── quadratic-function-vertex.md
│   ├── quadratic-function-graph.md
│   └── circle-introduction.md
│
└── exercise/
    ├── quadratic-function.yml
    └── circle.yml


server/
└── api/
    ├── chapter/
    │   ├── index.get.ts
    │   └── [slug].get.ts
    │
    ├── lesson/
    │   └── [slug].get.ts
    │
    └── exercise/
        └── [slug].get.ts


repositories/
├── ChapterRepository.ts
├── LessonRepository.ts
└── ExerciseRepository.ts
```

这套结构与当前的路由设计、学习流程、章节学习模式以及未来扩展方向保持一致。
